<%= simple_form_for [@ticket, @history] do |f| %>
  <%= f.input :comment %>
  <%= f.submit 'Aprovar', class: 'btn btn-default' %>
  <%= f.submit 'Reprovar', class: 'btn btn-default' %>
  <%= link_to 'voltar', user_path(@current_user), class: 'btn btn-default' %>
<% end %>
